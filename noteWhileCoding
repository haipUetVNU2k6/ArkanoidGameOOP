----
Bước tiếp theo: chuyển từ static nameSate sang các state có vòng đời rõ ràng, ví dụ:

    stateStack.push(new MenuState());
    stateStack.push(new PlayState(level));

Todo List:
    1, xóa các biến static State ở mainApp.
    2, check hàm pop ở stateStack có cần xóa state ko
    3, tại mỗi stateCtrl, push(new State).
    4. sử dụng onEnter, onExit

06_11_25  11:35AM: done
----

----
Next: PlayState(int level)

----
Next: lossLevelState
Note: hiện đang có lỗi:
    khi brick cuối cùng bị phá, chưa kịp render ảnh brick đấy mất thì đã hiện
    winlevelState.

    -> dự kiến fix: dùng flag

đề xuất: update win level: in case newRecord
